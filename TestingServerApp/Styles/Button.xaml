<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type Button}" x:Key="Style_MenuButtonBase">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="5 5 0 0"/>
        <Setter Property="Width" Value="90"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="Style_ExitButton" BasedOn="{StaticResource Style_MenuButtonBase}">
        <Setter Property="Margin" Value="5 5 5 5"/>
        <Setter Property="Width" Value="25"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <!-- prevent button color change on mouseover -->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid Background="{TemplateBinding Background}" >
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <!-- background image -->
        <Setter Property="Content">
            <Setter.Value>
                <Image Source="\Images\CloseButtonImage.png"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Content">
                    <Setter.Value>
                        <Image Source="\Images\CloseButtonImageMouseOver.png"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- The style is intended to multiple datagrids on different pages. To work properly datagrid name must be "DataGrid"  -->
    <Style TargetType="{x:Type Button}" x:Key="Style_MenuVisibleOnSelectionButton" BasedOn="{StaticResource Style_MenuButtonBase}">
        <Setter Property="IsEnabled" Value="True"></Setter>
        <Style.Triggers>
            <DataTrigger Binding ="{Binding ElementName=DataGrid, Path=SelectedIndex}" Value="-1">
                <Setter Property="IsEnabled" Value="False"></Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- The style is intended to multiple datagrids on different pages. To work properly datagrid name must be "DataGrid"  -->
    <Style TargetType="{x:Type Button}" x:Key="Style_MenuVisibleOnSelectionButton2" BasedOn="{StaticResource Style_MenuButtonBase}">
        <Setter Property="IsEnabled" Value="True"></Setter>
        <Style.Triggers>
            <DataTrigger Binding ="{Binding ElementName=DataGrid2, Path=SelectedIndex}" Value="-1">
                <Setter Property="IsEnabled" Value="False"></Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>